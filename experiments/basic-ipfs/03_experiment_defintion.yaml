apiVersion: int.esa.yass/v1
kind: ExperimentDefinition
metadata:
  name: basic-ipfs-experimentdef
spec:
  maxDuration: 90m
  behaviours:
    - fsNode: sat-1
      agent:
        image: nginx
        envsMap:
          foo: bar
      hardwareEvents: []

    - fsNode: sat-2
      agent:
        image: nginx
      hardwareEvents: []

    - fsNode: sat-3
      agent:
        image: nginx
      hardwareEvents: []

    - fsNode: ground-station-01
      agent:
        image: nginx
      hardwareEvents: []
