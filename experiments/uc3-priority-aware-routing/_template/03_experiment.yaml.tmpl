apiVersion: int.esa.yass/v1
kind: Experiment
metadata:
  name: ${RUN_ID}
  namespace: ${NAMESPACE}
spec:
  # Pinned simulationStartTime shared with UC1 so the three priority
  # runs at each sat_count see bit-identical orbital geometry.
  simulationStartTime: "2026-05-16T23:59:00.000Z"
  experimentDefRef: ${RUN_ID}
  layoutDefRef: ${LAYOUT_REF}
  runId: "${RUN_ID}"
${ENGINE_CONTAINERS}
  start: true
